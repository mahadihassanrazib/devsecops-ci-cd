apiVersion: v1
kind: Service
metadata:
  name: yourmentors-frontend
  namespace: crud-app
  labels:
    app: yourmentors-frontend
spec:
  selector:
    app: yourmentors-frontend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: LoadBalancer
